<%= form_with(model: @favorited_brand, local: true) do |f| %>

  <%= f.hidden_field :brand_id, value: @user.id %>

  <%= f.submit "Create favorited brand", class: "btn btn-block btn-outline-secondary" %>
<% end %>
